import React from "react";
import Navbar from "../Navbar/Navbar";

const Chat = ({ auth }) => {
  return (
    <>
      <Navbar auth={auth} />

      {/* <Users /> */}
      {/* <Messages /> */}
    </>
  );
};

export default Chat;
